/**
 * Landing Layout — Full-width marketing layout.
 * No sidebar or topbar — clean, immersive experience for the landing page.
 */
export default function LandingLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return <>{children}</>;
}
